[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"

# reMarkable 2 (armv7 hard-float). Static musl so the binary runs on the
# device's older glibc regardless of the build host's cross toolchain.
[target.armv7-unknown-linux-musleabihf]
linker = "rust-lld"
